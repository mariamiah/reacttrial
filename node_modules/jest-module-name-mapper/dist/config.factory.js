"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = require("chalk");
const fs = require("fs");
const getConfigPath = (configPath) => !!configPath
    ? `${process.cwd()}/${configPath}`
    : `${process.cwd()}/tsconfig.json`;
exports.getConfig = (configPath) => {
    const tsconfigPath = getConfigPath(configPath);
    if (!fs.existsSync(tsconfigPath)) {
        console.error(chalk_1.default.red(`${tsconfigPath} file does not exist`));
        process.exit();
    }
    return require(tsconfigPath);
};
//# sourceMappingURL=config.factory.js.map